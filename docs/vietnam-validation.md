# Проверка staging, 15 сентября 2026

## До push

- Базовая проверка исходного b9210c9: 65 страниц, `npm run check` — PASS.
- После изменений `npm run build`: 66 страниц — PASS.
- `npm run check`: ссылки, изображения, title/canonical/Open Graph, landmarks, H1, ID/якоря, типографика и шрифты — PASS на всех 66 страницах.
- Дополнительно проверены 14 вьетнамских страниц и статей: уникальные title, canonical в sitemap, отсутствие Tripadvisor Дахаба, отсутствие noindex в пользовательском HTML, последняя крошка соответствует canonical — PASS.
- Sitemap: 65 уникальных канонических URL, без 404/технических адресов. Новая страница дороги включена, существующие .php статьи сохранены — PASS.
- Сравнение main content 44 остальных страниц: вне страниц контактов изменений нет. Общие формы и метаданные обновлены штатным генератором.
- 5 изолированных групп тестов: серверная валидация, honeypot/Origin/лимит размера, недоступная конфигурация, успех и повторы/конкуренция, отказ/таймаут/лимит частоты, клиентский контракт и privacy событий — PASS. Реальных отправок в Telegram не выполнялось.
- `git diff --check`: PASS; удалений и обнаруженных шаблонов токенов в diff нет. `.env.example` пустой, секретов нет.
- Реальный исходный /vietnam/ открыт в браузере; старая страница проката проверена по живой таблице, подтверждены расхождения цен и ошибочные tel href старого сайта.

## Ограничения

Локальный URL 127.0.0.1 браузер этой среды не открывает (ERR_BLOCKED_BY_CLIENT). Браузерную проверку изменённого сайта выполнять на реальном deployment после push. Текущий браузер имеет viewport 1363×936, API смены viewport не предоставлен; нельзя считать это проверкой 390 px. Node-тесты клиента не заменяют визуальную мобильную проверку.

Настройки секретов/D1 и реальная доставка пока не проверены; для включения см. contact-setup.md. Показатели Lighthouse/скорости не измерялись.
